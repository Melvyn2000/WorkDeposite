{% extends 'base.html.twig' %}

{% block title %}Works{% endblock %}

{% block body %}
    <h1>Works</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ work.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ work.description }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ work.img }}</td>
            </tr>
            <tr>
                <th>FilesOrlinks</th>
                <td>{{ work.filesOrlinks }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_works_index') }}">back to list</a>

    <a href="{{ path('app_works_edit', {'id': work.id}) }}">edit</a>

    {{ include('works/_delete_form.html.twig') }}
{% endblock %}
